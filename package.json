{
  "name": "dm99v2-ai",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "AI-powered techno and electronic drum machine",
  "scripts": {
    "dev": "node scripts/dev-server.js",
    "build": "node --check script.js && node --check api/generate-pattern.js && node scripts/build-static.js",
    "start": "node scripts/dev-server.js",
    "test": "node --test"
  },
  "engines": {
    "node": "22.x"
  }
}
